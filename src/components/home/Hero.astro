---
import me from "@/assets/me.png"
import { Image } from "astro:assets"
import ContactButton from "../ContactButton.astro"
import Iso from "../Iso.astro"
import SocialLinks from "../SocialLinks.astro"
import { Typography } from "../ui/typography"
import Announcement from "./Announcement.astro"
import Skills from "./Skills.astro"
import { PROFILE } from "@/constants/profile"
---

<header class="mt-20 flex flex-col gap-y-20 overflow-hidden py-20">
  <figure
    class="container flex flex-col-reverse items-center justify-center gap-28 xl:flex-row-reverse"
  >
    <div class="relative hidden w-full max-w-md xl:block">
      <Iso
        class="animate-draw stroke-primary absolute inset-0 [stroke-dasharray:1000]"
      />
      <Image
        src={PROFILE.photo}
        alt={`Photo of ${PROFILE.name}, ${PROFILE.title}`}
        class="relative h-full w-full mask-radial-[100%_100%] mask-radial-from-75% mask-radial-at-top-left object-cover"
        loading="eager"
      />
    </div>
    <figcaption
      class="space-y-6 text-center sm:max-w-lg md:max-w-xl xl:text-left"
    >
      <Announcement />
      <Typography variant="h1">
        {PROFILE.title}{" "}
        <span class="text-primary">for the AI Era</span>
      </Typography>
      <Typography variant="p">
        {PROFILE.bio}
      </Typography>
      <div
        class="flex flex-wrap items-center justify-center gap-2 xl:justify-start"
      >
        <ContactButton />
        <SocialLinks />
      </div>
    </figcaption>
  </figure>
  <Skills />
</header>
